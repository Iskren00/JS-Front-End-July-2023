function solve(n) {
    for (let index = 0; index < n; index++) {
        let array = [ ];

        for (let index = 0; index < n; index++) {
                        array.push(n);
        }

        console.log(array.join(' '));
    }
}

solve(7);